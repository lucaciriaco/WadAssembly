<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="CommunityWadCompiler.App.Views.MainWindow"
        Title="Community Wad Compiler"
        Width="1180" Height="760"
        MinWidth="900" MinHeight="600"
        WindowStartupLocation="CenterScreen">

  <DockPanel>

    <Menu DockPanel.Dock="Top">
      <MenuItem Header="Archivo">
        <MenuItem Header="Nuevo proyecto" Click="OnNewProject"/>
        <Separator/>
        <MenuItem Header="Abrir proyecto..." Click="OnOpenProject"/>
        <MenuItem Header="Guardar proyecto como..." Click="OnSaveProjectAs"/>
        <Separator/>
        <MenuItem Header="Salir" Click="OnExit"/>
      </MenuItem>
      <MenuItem Header="Ayuda">
        <MenuItem Header="Acerca de..." Click="OnAbout"/>
      </MenuItem>
    </Menu>

    <Grid DockPanel.Dock="Bottom" ColumnDefinitions="Auto,*" Margin="8,4,8,8">
      <ProgressBar Grid.Column="0" Width="260" Height="18" IsIndeterminate="True"
                   IsVisible="{Binding IsBusy}" VerticalAlignment="Center"/>
      <Button Grid.Column="1" HorizontalAlignment="Right" Content="Compilar"
              IsDefault="True" Click="OnCompile" MinWidth="120" Margin="8,0,0,0"/>
    </Grid>

    <Grid Margin="8" RowDefinitions="*,Auto,Auto,*" ColumnDefinitions="*,2*">

      <!-- Left: input WADs + resource WADs -->
      <Grid Grid.Row="0" Grid.Column="0" RowDefinitions="*,*" Margin="0,0,8,8">

        <DockPanel Grid.Row="0">
          <TextBlock DockPanel.Dock="Top" Text="WADs aportados (mapas)" FontWeight="Bold" Margin="0,0,0,4"/>
          <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Spacing="6" Margin="0,6,0,0">
            <Button Content="Agregar WADs..." Click="OnAddWads"/>
            <Button Content="Quitar" Click="OnRemoveWad"/>
            <Button Content="Subir" Click="OnMoveUp"/>
            <Button Content="Bajar" Click="OnMoveDown"/>
          </StackPanel>
          <ListBox ItemsSource="{Binding InputWads}"
                   SelectedItem="{Binding SelectedWad, Mode=TwoWay}">
            <ListBox.ItemTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding Display}"/>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
        </DockPanel>

        <DockPanel Grid.Row="1" Margin="0,8,0,0">
          <TextBlock DockPanel.Dock="Top" Text="WAD de recursos (texturas / flats)" FontWeight="Bold" Margin="0,0,0,4"/>
          <StackPanel DockPanel.Dock="Bottom" Orientation="Vertical" Spacing="4" Margin="0,6,0,0">
            <CheckBox Content="Solo incluir texturas/flats usados por los mapas"
                      IsChecked="{Binding FilterResourcesToUsed}"/>
            <StackPanel Orientation="Horizontal" Spacing="6">
              <Button Content="Agregar recursos..." Click="OnAddResources"/>
              <Button Content="Quitar" Click="OnRemoveResource"/>
              <Button Content="Subir" Click="OnMoveResourceUp"/>
              <Button Content="Bajar" Click="OnMoveResourceDown"/>
            </StackPanel>
          </StackPanel>
          <ListBox ItemsSource="{Binding ResourceWads}"
                   SelectedItem="{Binding SelectedResourceWad, Mode=TwoWay}">
            <ListBox.ItemTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding Display}"/>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
        </DockPanel>

      </Grid>

      <!-- Right: maps / slots -->
      <DockPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,8">
        <TextBlock DockPanel.Dock="Top" Text="Mapas y slots finales (editable)"
                   FontWeight="Bold" Margin="0,0,0,4"/>
        <Grid DockPanel.Dock="Top" ColumnDefinitions="150,110,80,110,200,140,150" Margin="0,0,0,2">
          <TextBlock Grid.Column="0" Text="WAD" FontWeight="Bold"/>
          <TextBlock Grid.Column="1" Text="Mapa original" FontWeight="Bold"/>
          <TextBlock Grid.Column="2" Text="Formato" FontWeight="Bold"/>
          <TextBlock Grid.Column="3" Text="Slot final" FontWeight="Bold"/>
          <TextBlock Grid.Column="4" Text="Nombre del mapa" FontWeight="Bold"/>
          <TextBlock Grid.Column="5" Text="Música" FontWeight="Bold"/>
          <TextBlock Grid.Column="6" Text="Autor" FontWeight="Bold"/>
        </Grid>
        <Border BorderBrush="#808080" BorderThickness="1" CornerRadius="3">
          <ScrollViewer ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Maps}">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Grid ColumnDefinitions="150,110,80,110,200,140,150" Margin="2,3">
                    <TextBlock Grid.Column="0" Text="{Binding WadFileName}"
                               TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <TextBlock Grid.Column="1" Text="{Binding OriginalName}"
                               VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <TextBlock Grid.Column="2" Text="{Binding FormatLabel}"
                               VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <TextBox Grid.Column="3" Text="{Binding FinalName, Mode=TwoWay}"
                             MinWidth="60" VerticalContentAlignment="Center"/>
                    <TextBox Grid.Column="4" Text="{Binding LevelName, Mode=TwoWay}"
                             MinWidth="110" VerticalContentAlignment="Center"/>
                    <ComboBox Grid.Column="5"
                              ItemsSource="{Binding MusicOptions}"
                              SelectedItem="{Binding MusicName, Mode=TwoWay}"
                              VerticalAlignment="Center" MinWidth="130"
                              MaxDropDownHeight="220" PlaceholderText="Ninguna"/>
                    <TextBox Grid.Column="6" Text="{Binding Author, Mode=TwoWay}"
                             MinWidth="120" VerticalContentAlignment="Center"/>
                  </Grid>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </ScrollViewer>
        </Border>
      </DockPanel>

      <!-- Base WAD -->
      <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" ColumnDefinitions="Auto,*,Auto,Auto" Margin="0,0,0,8">
        <TextBlock Grid.Column="0" Text="WAD base (IWAD):" VerticalAlignment="Center" Margin="0,0,8,0"/>
        <TextBox Grid.Column="1" Text="{Binding BaseWadPath, Mode=TwoWay}" Watermark="Opcional. DOOM2.WAD como semilla para TEXTURE1/PNAMES."/>
        <Button Grid.Column="2" Content="..." Click="OnBrowseBaseWad" Margin="4,0,0,0"/>
        <Button Grid.Column="3" Content="Limpiar" Click="OnClearBaseWad" Margin="4,0,0,0"/>
      </Grid>

      <!-- Output -->
      <Grid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" ColumnDefinitions="Auto,*,Auto" Margin="0,0,0,8">
        <TextBlock Grid.Column="0" Text="Salida:" VerticalAlignment="Center" Margin="0,0,8,0"/>
        <TextBox Grid.Column="1" Text="{Binding OutputPath, Mode=TwoWay}" Watermark="Ruta del .wad compilado (ej. megawad.wad)"/>
        <Button Grid.Column="2" Content="..." Click="OnBrowseOutput" Margin="4,0,0,0"/>
      </Grid>

      <!-- Log -->
      <Grid Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" RowDefinitions="Auto,*">
        <TextBlock Text="Registro" FontWeight="Bold" Margin="0,4,0,4"/>
        <TextBox Grid.Row="1" Text="{Binding LogText, Mode=OneWay}"
                 IsReadOnly="True" TextWrapping="Wrap"
                 AcceptsReturn="True" FontFamily="Consolas, Courier New"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"/>
      </Grid>

    </Grid>
  </DockPanel>
</Window>